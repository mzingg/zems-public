<?xml version="1.0" encoding="UTF-8" ?>
<settings
  xmlns="http://maven.apache.org/SETTINGS/1.2.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd"
>
  <!--
    Release-time Maven settings for the public repo's deploy pipeline (.github/workflows/release.yml). The deploy runs
    the release profile against this file (passed with the Maven -s option), inside the nix release shell.

    No secrets live in this file. The Central Portal token is read from the environment
    (CENTRAL_TOKEN_USERNAME / CENTRAL_TOKEN_PASSWORD), which CI injects from GitHub secrets. The GPG passphrase is
    read separately by maven-gpg-plugin from MAVEN_GPG_PASSPHRASE.
  -->
  <servers>
    <server>
      <id>central</id>
      <username>${env.CENTRAL_TOKEN_USERNAME}</username>
      <password>${env.CENTRAL_TOKEN_PASSWORD}</password>
    </server>
  </servers>
</settings>
